# Info 

AM Database is a database that uses kvp to store everything, 
a database object works similar to a lua table. 

when calling a database object no values are cached only the keys. 
values are being cached when called up on and can be unloaded, loaded and destroyed 
by using methods. 